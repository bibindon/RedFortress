# STAGE_1_7「つながれ！バラバラとりで」設計書（全面刷新版）

## 基本情報

| 項目 | 値 |
|---|---|
| ワールド | World 1（草原・昼） |
| サイズ | 30×60m（X: −16〜+16, Z: −32〜+32） |
| スタート位置 | `(0, 0.2, 28)` |
| ゴール位置 | `(0, 1.0, -28)` |
| 地面モデル | `stage_ground.x`（中央の全幅堀：X −14.8〜+14.8, Z −5〜+5, 深さ20m） |
| 空モデル | `../SkySphere/SkySphere.blend.x` |
| 設計方針 | 中央の堀（落とし穴）が砦を北と南に分断。移動床フェリーで堀を渡り、攻略順を選べる。木箱・岩で足場を補い、破壊・レバーで道を開く |

## 区画構成（3区画）

テーマ「離れた砦区画を移動床でつなぎ、どの区画から攻略するかを選べる」。中央の堀が自然な分断となっている。

| # | 区画 | Z範囲 | 主目的 |
|---|---|---|---|
| S1 | 北の砦 | +5〜+30 | スタート・序盤戦闘・QTE木・ダッシュ床→高所台座 |
| S2 | 中央の堀 | −5〜+5 | 移動床フェリー2基で渡る分断（メイン横断） |
| S3 | 南の砦 | −30〜−5 | 中盤〜終盤戦闘・レバー近道・スター・ゴール |

## 必須ギミック配置

1. **移動床 — 2個（メイン横断フェリー）**
   | ID | 移動 | 用途 |
   |---|---|---|
   | 3511 | `(0,0.4,6) → (0,0.4,-1)` 水平（Scale1） | 北岸→堀中央へ |
   | 3512 | `(0,0.4,1) → (0,0.4,-6)` 水平（Scale1） | 堀中央→南岸へ |
   - 描画 `../collision_moving_platform/collision_moving_platform.x` / 物理 `res/model/collision_moving_platform.x`（Move=y）を同じIDで登録。XFileListMove.csv と一致。
   - スイープが連続（北 6→−1 と 南 1→−6 がZ=0付近で重なる）ため、`_build_stage_grounds.py` の安全ルート検証を通過しながら「ゴールへたどり着ける」を達成。

2. **QTE木 — 1個（北の砦）**
   `stage17-tree-01` @ `(6, 0, 24)`、PromptDistance 2.5。描画 `../tree2/lemonTree.x`（3801）、衝突 `../tree2Physics/tree_cylinder_collision.x`（3801）、`Interactables.csv`。成功でランダム素材1個。

3. **ダッシュブースター — 1個（北の砦→高所台座）**
   | ID | 位置 | 方向 | Speed | 用途 |
   |---|---|---|---|---|
   | stage17-booster-01 | `(5, 0.5, 8)` | `(0, 0.93, -0.37)` 上・南斜め | 16 | 堀上の高所台座3620へ（ショートカット＋アイテムI04取得） |

4. **Y座標3以上の動かない床 — 1個（堀上）**
   | ID | モデル | 位置 | 到達手段 | 配置物 |
   |---|---|---|---|---|
   | 3620 | `static_platform_2x2`（6×6m） | `(5, 3.4, 3)` | ダッシュ床booster-01 | アイテムI04 |
   - 堀（ピット）上に設置するため、`_build_stage_grounds.py` の STAGES 定義に `static_platforms=((5.0, 3.0, 3.0),)` を追加しないと、台座上の収集物が「ピット内に配置」扱いで検証エラーになる。**必須対応**。
   - 台座とフェリー3511/3512（X=0）はX座標をずらし（X=5）、衝突を避ける。

5. **レバー — 1個（南の砦、隠し収集物への近道）**
   `Lever` @ `(4, 0.6, -16)`、TargetID=11001、Axis=Y。連動 `attack_wall` 11001 `(9, 1.5, -12)` Scale=2、物理側 `Move=y`。壁をY軸回転させて隠し収集物I05への近道を開ける（正規ルートには影響なし）。

6. **ダメージ床 — 2個（堀の両端の川岸）**
   | PhysicsID | 位置 | 用途 |
   |---|---|---|
   | 3701 | `(-10, 0.02, 6.2)` | 堀西岸（地面側）を危険化しフェリーへ誘導 |
   | 3702 | `(10, 0.02, 6.2)` | 堀東岸（地面側）を危険化しフェリーへ誘導 |
   描画 `../plateLava.x` / 物理 `res/model/plateLava.x`（Type=NonCollision）を両CSVへ。`LavaZones.csv` で PhysicsID を指定（Damage 20）。フェリー経路（X=0）には置かない（北岸の地面側 Z=6.2 に設置）。※ピット内配置は地面検証で除去される。

7. **スター** — `(0, 0.45, -9)`（堀を渡った南側で無敵・最高速）

8. **スピードアップ** — `(8, 0.45, -16)`（南の砦）

9. **ポイントライト** — 昼の明るさを維持しつつ拠点らしさを出す軽量灯を2基程度。

## 端のギミック（2つ以上、開始・ゴール周辺以外）

- 北東端：収集物I01 `(10, 0.45, 26)`
- 南東端：収集物I03 `(9, 0.45, -27)`
- 柵外には木・岩・草のインスタンシング景観（後述）

## 敵配置（EnemyPositions.csv）— World 1: 11体

World 1方針: `wolf`・`small_mushroom`・`crab`・`frog` のみ（birdは1-4以降・少数のみ）。ゴースト/クモ/スケルトン/ゴーレム/大型は不使用。

| 区画 | 配置 | 数 |
|---|---|---|
| 北の砦 | wolf(-10,20), frog(9,19), crab(-9,15), small_mushroom(8,12), wolf(3,7) | 5 |
| 南の砦 | small_mushroom(-7.5,-10), crab(9,-14), frog(4,-18), wolf(-5,-22), small_mushroom(8,-26) | 5 |
| 堀上空 | bird(0,2,-15) | 1 |

**計11体**（要件10体超）。地上敵は Y=0.2、飛行 bird は Y=2.0。

## 収集物（Collectibles.csv）— 6個

| ID | DataID | 位置 | 到達手段 |
|---|---|---|---|
| stage17-I01 | 001 | `(10, 0.45, 26)` | 北東端（端ギミック） |
| stage17-I02 | 003 | `(-6, 1.45, 24)` | 北の木箱の上（QTE木周辺） |
| stage17-I03 | 004 | `(9, 0.45, -27)` | 南東端（端ギミック） |
| stage17-I04 | 005 | `(5, 3.85, 3)` | 高所台座3620（ダッシュ床で） |
| stage17-I05 | 006 | `(13, 0.45, -16)` | レバーで開く隠し近道 |
| stage17-I06 | 009 | `(-4, 0.45, 10)` | 北の砦 西 |

DataIDに `007`/`008`（異常終了）は使用しない。

## その他の配置

- 破壊可能木箱（Destructibles.csv）— 3個: `(-7,0.45,-26)`・`(7,0.45,-20)`・`(-9,0.45,-9)`。HPは2〜3、うち1個はDropItemId付き。
- 木箱（cubeWoodSmall）・岩（base_rock1/2）— 北・南両ゾーンの足場・遮蔽として配置。
- 柵（fence）— 外周（南北 Z=±30、側面 X=±15）に配置。当たり判定なしの目印。
- 壁（collision_wall）— ゾーン境界の目印と遮蔽に数枚。安全ルートは塞がない。
- 柵外景観（World 1: 木・岩・草）— インスタンシングで大量配置。
  - 草: `res/model/grass/grass1-7.csv`（スウェイ）
  - 木: `res/model/tree2/lemonTree.Instancing.1-7.csv`
  - XFileList_simple.csv に `loadType=instancing` と PlacementCsv（ID 9200系）を登録。

## STAGES 定義への変更（_build_stage_grounds.py）

現行 1-7 エントリ（line 94）に次を追加：
- `static_platforms=((5.0, 3.0, 3.0),)`  ← 堀上の高所台座3620を認識させる（必須）
- 移動床フェリー3511/3512のスイープが連続するため、`jump_links` 追加は不要（安全ルートは移動床で確保済み）。

## 地面モデル

- `stage_ground.x` は現行の中央堀（X −14.8〜+14.8, Z −5〜+5, 深さ20m）を維持。
- `RED_FORTRESS_STAGE_GROUND='1-7'` で `_build_stage_grounds.py` をBlender実行して検証・再生成。

## 完了条件チェック

- [x] 通常走行・ジャンプ・近接攻撃だけで開始→全必須敵撃破→ゴール到達
- [x] 必須敵は主要導線上・隠し場所なし・ピット上なし
- [x] 開始(G-北)・ゴール(G-南)周辺に回避不能ダメージなし
- [x] QTE木・ダッシュ床・移動床・レバー・ダメージ床・Y≥3静的床（アイテム）をすべて1以上
- [x] 敵11体≥10
- [x] 区画3（World 1目安「約3」）
- [x] 端ギミック2以上（北東I01・南東I03）
- [x] 描画/物理CSVの座標・回転・倍率一致
- [x] 柵外に木・岩・草のインスタンシング景観

## ビルド・検証

- `MSBuild ... /p:Configuration=Debug /p:Platform=x64` → 0エラー
- 静止検証スクリプトで 13 項目チェック（重複ID・描画↔物理整合・移動床3CSV連携・LavaZones PhysicsID・AttackTriggers TargetID・敵数/型・収集物DataID・ギミック最小数・開始ゴール7m以内に敵なし）
- ※ビルド＋静的チェックはゲーム内挙動（フェリー乗り継ぎ・高台への射出・安全ルート）を検証しない。手動プレイテスト必須。

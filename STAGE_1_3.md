# STAGE_1_3「ガレキでふさがれた道」設計書 (地面なしステージ版)

## 基本情報

| 項目 | 値 |
|---|---|
| ワールド | World 1（草原・昼） |
| ジャンル | **地面なしステージ**（地面はすべて落とし穴。動く床・動かない床で足場を構築） |
| サイズ | 30×60m（X: −16〜+16, Z: −32〜+32） |
| スタート位置 | `(0, 0.2, 28)` — 北端の足場の上 |
| ゴール位置 | `(0, 1.0, -28)` — 南端の足場の上 |
| 地面モデル | `stage_ground.x`（全面ピット。外周 1.5m はピットの縁として地面が残る） |
| 設計方針 | ガレキ（破壊可能木箱）が足場上の道を塞いでいる。**壊さないと先へ進めない**。レバー3・感圧板3（箱＋上下する壁）は新ルール準拠の必須ギミックとして寄り道の島に配置。 |

## 地面・足場（STAGES 定義）

- `pits`: `((-14.5, 14.5, -30.5, 30.5),)` — 全面ピット（ピットは外壁に触れられないため外周 1.5m の地面帯が残る。外周帯はガレキで道を塞ぐ）
- `static_platforms`: 下記の足場を登録（start/goal も足場の上）

### 足場の島（静的プラットフォーム）

| 島 | 位置 | モデル | 役割 |
|---|---|---|---|
| S（スタート） | `(0, 26, 3.0)` | 2x2（6x6m） | スタート足場 |
| A | `(0, 21, 1.5)` | 1x1（3x3m） | 中継 |
| B | `(0, 16, 1.5)` | 1x1 | 中継（ガレキで塞ぐ） |
| C | `(0, 11, 1.5)` | 1x1 | 中継 |
| D（レバー3島） | `(0, 4, 3.0)` + `(0, 8, 3.0)` | 2x2×2（12x6m） | レバー3の箱＋前庭 |
| E | `(0, -2, 1.5)` | 1x1 | 中継 |
| F | `(0, -10, 1.5)` | 1x1 | フェリー南岸 |
| G（感圧板3島） | `(0, -16, 3.0)` + `(0, -12, 3.0)` | 2x2×2（12x6m） | 感圧板3の箱＋前庭 |
| H | `(0, -21, 1.5)` | 1x1 | 中継（ガレキで塞ぐ） |
| I（ゴール） | `(0, -26, 3.0)` | 2x2（6x6m） | ゴール足場 |

- 島の間隔は 4〜5m（通常ジャンプの水平 5.5m 以内）に統一。
- レバー3島 D と感圧板3島 G は 12x6m の島。箱（6x6x6m）を東側（X +3〜−3）に置き、**西側（X −6〜−3）に 3m の通路**を残す（BFS は箱を迂回できる）。
- 外周の地面帯（幅 1.5m）は、ガレキの山で随所を塞ぐ。

## 区画構成（3区画）

| # | 区画 | Z範囲 | 主目的 |
|---|---|---|---|
| S1 | 北の瓦礫広場 | +30〜+8 | スタート・ガレキの壁を壊して道を開く・QTE木 |
| S2 | 中央の渡し | +8〜−8 | 移動床フェリーで横断・レバー3（箱）の寄り道 |
| S3 | 南の瓦礫広場 | −8〜−30 | 感圧板3（箱）・ゴール前のガレキ・Y≥3台座・ゴール |

## 必須ギミック配置

1. **移動床 — 1個（中央のフェリー）**
   | ID | 移動 | 用途 |
   |---|---|---|
   | 3511 | `(0,0.4,-3) → (0,0.4,-8)` 垂直（Scale 1.5） | 島E（z=−2）と島F（z=−10）の間のピット帯を渡る。スイープ帯（X ±2.7, Z −8〜−3）がピット帯を覆い安全ルートが通る |
   - 描画 `../collision_moving_platform/collision_moving_platform.x` / 物理 `res/model/collision_moving_platform.x`（Move=y）を同じID 3511 で登録。

2. **ダッシュブースター — 1個（島F→高所台座）**
   | ID | 位置 | 方向 | Speed | 用途 |
   |---|---|---|---|---|
   | stage13-booster-01 | `(8, 0.5, -10)` | `(0, 0.93, -0.37)` 上・南斜め | 16 | 高所台座 3620 へ（I04取得） |
   - 島F の東端から高所台座（Y 3.4）へ射出。

3. **Y座標3以上の動かない床 — 1個（ピット上）**
   | ID | モデル | 位置 | 到達手段 | 配置物 |
   |---|---|---|---|---|
   | 3620 | `static_platform_1x1`（3x3m） | `(8, 3.4, -14)` | ダッシュ床 booster-01 | アイテムI04 |
   - ピット上に設置するため `static_platforms` に `(8.0, -14.0, 1.5)` を追加（必須対応）。

4. **QTE木 — 1個（北西のガレキのアルコーブ）**
   `stage13-tree-01` @ `(-6, 0, 24)`、PromptDistance 2.5。描画 `../tree2/lemonTree.x`（3801）、衝突 `../tree2Physics/tree_cylinder_collision.x`（3801）、`Interactables.csv`。入口は破壊可能ガレキ2個で塞ぎ、壊すと見つかる構成。成功でランダム素材1個。

5. **レバー3（両開き門）— 1セット（島Dの寄り道）**
   - 箱 `lever_box3.x` @ `(0, 0.1, 6)`（ID 9103）、扉 `lever_box3_door.x` @ `(0, 0.1, 6)`（ID 9104, Move=y, Scale 0.98）、床 `lever_box3_floor.x`（5x10x1.0）@ `(0, -0.9, 6)`（ID 9102）
   - レバー2つ（AttackTriggers ID=3, 4）: 各連動壁の前 `(0, 0.7, 9)` / `(0, 0.7, 3)`。どちらを操作しても両扉が同時に開閉（共有トグル）。
   - 箱の中にアイテム I05。箱の西側（X −6〜−3）が通路。

6. **感圧板3（両開き門）— 1セット（島Gの寄り道）**
   - 箱 `lever_box3.x` @ `(-10, 0.7, -18)`（ID 9113）、扉 `lever_box3_door.x` @ `(-10, 0.7, -18)`（ID 9114, Move=y, Scale 0.98）、床 `lever_box3_floor14.x`（5x14x1.0）@ `(-10, -0.3, -18)`（ID 9112）
   - 感圧板3つ（PressurePlates ID=1, 2, 3）: 各扉の外側 `(-10, 0.71, -14)` / `(-10, 0.71, -22)` と、箱内中央 `(-10, 1.21, -18)`。箱内板は箱モデルの0.5m厚の内蔵床上面から1cm上に置く。どの感圧板に乗っても両扉が同時に開き、閉じ込められても箱内から再開放できる（共有WallID・OR）。
   - 箱の中にアイテム I06。箱の西側が通路。

7. **破壊可能ガレキ（テーマ）— 道を塞ぐ**
   - 島B→C間（z=14 付近）: `(-0.8, 0.45, 14.2)` `(0.8, 0.45, 14.2)` — 3x3m 島の中央を塞ぐ。壊さないと先へ進めない。
   - 島H→I間（ゴール前、z=-23 付近）: `(-0.8, 0.45, -23.2)` `(0.8, 0.45, -23.2)` — **ゴール前の最後のガレキ。壊さないとゴールに到達できない。**
   - 外周帯の北東・南西: ガレキの山で進入を塞ぐ。
   - QTE木アルコーブ入口: `(-4.5, 0.45, 23.5)` `(-5, 0.45, 25)`

8. **スター** — `(0, 0.45, -24)`（ゴール前）
9. **スピードアップ** — `(0, 0.45, 18)`（北区画）

## 端のギミック（2つ以上）

- 北西端: QTE木 `(-6, 24)`
- 東側: ダッシュ床→高所台座（Y 3.4、I04）
- 南西端: スター `(0, -24)`・ガレキ
- レバー3島（東・西の寄り道）

## 敵配置（EnemyPositions.csv）— World 1: 11体

World 1方針: `wolf`・`small_mushroom`・`crab`・`frog` のみ。

| 区画 | 配置（足場の上、PosY 0.2） | 数 |
|---|---|---|
| 北 | wolf(0,26), small_mushroom(-6,21), crab(0,16), frog(6,11), wolf(0,11) | 5 |
| 中央〜南 | crab(0,-2), small_mushroom(0,-10), frog(0,-16), wolf(0,-21), small_mushroom(-6,-26), crab(6,-26) | 6 |

**計11体**。すべて足場の上・ピット上なし・開始/ゴール 7m 圏内なし。

## 収集物（Collectibles.csv）— 6個

| ID | DataID | 位置 | 到達手段 |
|---|---|---|---|
| stage13-I01 | 001 | `(0, 0.45, 24)` | スタート島の端 |
| stage13-I02 | 003 | `(-6, 0.45, 22)` | QTE木そば |
| stage13-I03 | 004 | `(6, 0.45, 18)` | 島Cの東端（端ギミック） |
| stage13-I04 | 005 | `(8, 3.85, -14)` | 高所台座（ダッシュ床で） |
| stage13-I05 | 006 | `(0, 0.55, 6)` | レバー3の箱の中 |
| stage13-I06 | 009 | `(0, 0.55, -14)` | 感圧板3の箱の中 |

DataIDはゼロ埋め3桁（`007`/`008`は使用しない）。

## STAGES 定義への変更（_build_stage_grounds.py）

1-3 エントリを次に更新：
- `pits`: `((-14.5, 14.5, -30.5, 30.5),)` ← 全面ピット
- `static_platforms`: 全島 + 高所台座

## 完了条件チェック

- [ ] 通常走行・ジャンプ・近接攻撃だけで開始→ガレキを壊して→ゴール到達
- [ ] ガレキ（破壊可能オブジェクト）で道を塞ぎ、壊さないと先へ進めない（ゴール前もガレキ）
- [ ] レバー3・感圧板3（箱＋上下する壁）を新ルール準拠で配置
- [ ] QTE木・ダッシュ床・移動床・Y≥3静的床（アイテム）をすべて1以上
- [ ] 敵11体≥10、区画3、端ギミック2以上
- [ ] 描画/物理CSVの座標・回転・倍率一致
- [ ] ビルド（MSBuild Debug/x64）・起動確認

## ビルド・検証手順

1. `python tools/gen_stage_1_3_csvs.py` — CSV一式を生成（BOM付きUTF-8/CRLF）
2. `python tools/validate_stage_ground_only.py 1-3` — 地面検証（pit/配置/BFS安全ルート）
3. `RED_FORTRESS_STAGE_GROUND=1-3` で Blender 実行 → stage_ground.x 再生成＋検証
4. `MSBuild ... /p:Configuration=Debug /p:Platform=x64` → 0エラー
5. 起動確認（`--stage 1-3`）→ 手動プレイテスト

# STAGE_1_3「ガレキでふさがれた道」設計書

## 基本情報

| 項目 | 値 |
|---|---|
| ワールド | World 1（草原・昼） |
| サイズ | 30×60m（X: −16〜+16, Z: −32〜+32） |
| スタート位置 | `(0, 0.2, 28)` |
| ゴール位置 | `(0, 1.0, -28)` |
| 地面モデル | `stage_ground.x`（中央の堀：X −11〜+11, Z −13〜−7, 深さ20m） |
| 空モデル | `../SkySphere/SkySphere.blend.x` |
| 設計方針 | 破壊可能なガレキ（木箱）が道を塞いでいる。壊して進路を開拓しつつ、移動床フェリーで堀を渡り、感圧板で扉を開けてゴールへ進む。QTE木・レバー・ダッシュ床は寄り道の報酬と近道を担う |

## 区画構成（3区画）

テーマ「ガレキでふさがれた道」。北の砦跡 → 中央の堀 → 南の砦跡。区画はX両端（西: QTE木・レバー隠し部屋、東: 高所台座・収集物）へ分散させている。

| # | 区画 | Z範囲 | 主目的 |
|---|---|---|---|
| S1 | 北の砦跡（瓦礫の広場） | −7〜+30 | スタート・ガレキの壁を壊して道を開く・QTE木・レバー隠し部屋 |
| S2 | 中央の堀 | −13〜−7 | 移動床フェリーで横断（メイン横断）＋ダッシュ床→高所台座 |
| S3 | 南の砦跡 | −30〜−13 | 戦闘・感圧板の扉・スター・ゴール |

## 必須ギミック配置

1. **移動床 — 1個（中央の堀のフェリー）**
   | ID | 移動 | 用途 |
   |---|---|---|
   | 3511 | `(-6,0.4,-10) → (6,0.4,-10)` 水平（Scale2） | 堀の横断。スイープ（X −9〜+9, Z −13〜−7）が堀帯全域を覆うため `jump_links` 追加不要で安全ルートが通る |
   - 描画 `../collision_moving_platform/collision_moving_platform.x` / 物理 `res/model/collision_moving_platform.x`（Move=y）を同じID 3511 で登録。XFileListMove.csv と一致。

2. **ダッシュブースター — 1個（堀北岸→高所台座）**
   | ID | 位置 | 方向 | Speed | 用途 |
   |---|---|---|---|---|
   | stage13-booster-01 | `(10, 0.5, -6)` | `(0, 0.93, -0.37)` 上・南斜め | 16 | 堀上の高所台座3620へ（I04取得） |

3. **Y座標3以上の動かない床 — 1個（堀上）**
   | ID | モデル | 位置 | 到達手段 | 配置物 |
   |---|---|---|---|---|
   | 3620 | `static_platform_2x2`（6×6m） | `(10, 3.4, -10)` | ダッシュ床 booster-01 | アイテムI04 |
   - 堀（ピット）上に設置するため、`_build_stage_grounds.py` の STAGES 定義に `static_platforms=((10.0, -10.0, 3.0),)` を追加済み（必須対応）。

4. **QTE木 — 1個（北西、ガレキのアルコーブ）**
   `stage13-tree-01` @ `(-11.5, 0, 22)`、PromptDistance 2.5。描画 `../tree2/lemonTree.x`（3801）、衝突 `../tree2Physics/tree_cylinder_collision.x`（3801）、`Interactables.csv`。入口は破壊可能ガレキ2個で塞ぎ、壊すと見つかる構成。成功でランダム素材1個。

5. **レバー — 1個（北区画 西側）**
   `Lever` @ `(-11.5, 0, 11)`、TargetID=11001、Axis=Y。連動 `attack_wall` 11001 `(-11.5, 1.5, 14)` Scale=1、物理側 `Move=y`。壁がY軸回転して西側隠し部屋（I05・頭蓋骨）への近道が開く（正規ルートには影響なし）。

6. **感圧板＋連動扉 — 1個（南区画の進路）**
   | ID | 位置 | 用途 |
   |---|---|---|
   | 板 | `(0, 0.01, -19)` | 踏むと扉3106が上昇 |
   | 扉3106 | `(0, 1.5, -22)` RotY=90（描画 meshmix2 / 物理 Move=y） | ゴール前の最終区画を塞ぐ門。開けて先へ進む |
   - 近くに頭蓋骨 `(2.5, 0.2, -19)` を置き、ドクロを板に載せて扉を開いたままにする遊びも可能。

7. **破壊可能ガレキ（テーマ）— 9個**
   - メイン道のガレキの山: `(0,14)` `(0,11.5)` `(-2,13)` `(2,13)` — 壊して道を開く（HP2〜3、うち2個はDropItemId付き）。
   - QTEアルコーブ入口: `(-9,22)` `(-9.5,24)`
   - 南区画: `(7,-20)` `(-7,-17)` `(9,-24)`

8. **スター** — `(0, 0.45, -24)`（扉の先・ゴール前）
9. **スピードアップ** — `(0, 0.45, 18)`（北区画のメイン道）

## 端のギミック（2つ以上、開始・ゴール周辺以外）

- 北東端：収集物I01 `(10, 0.45, 26)`
- 南東端：収集物I03 `(13, 0.45, -25)`
- 北西端：QTE木 `(-11.5, 22)`
- 西側：レバー＋隠し部屋（I05・頭蓋骨）
- 柵外には木・岩・草のインスタンシング景観（後述）

## 敵配置（EnemyPositions.csv）— World 1: 11体

World 1方針: `wolf`・`small_mushroom`・`crab`・`frog` のみ。ゴースト/クモ/スケルトン/ゴーレム/大型は不使用。

| 区画 | 配置 | 数 |
|---|---|---|
| 北の砦跡 | wolf(-6,24), frog(8,20), small_mushroom(-3,14), crab(0,10), wolf(-9,6), frog(-7,2) | 6 |
| 南の砦跡 | crab(6,-16), small_mushroom(-8,-20), frog(11,-14), wolf(7,-26), small_mushroom(-8,-25) | 5 |

**計11体**（要件10体超）。地上敵は Y=0.2。すべて主要導線上・ピット上なし・開始/ゴール7m圏内なし。

## 収集物（Collectibles.csv）— 6個

| ID | DataID | 位置 | 到達手段 |
|---|---|---|---|
| stage13-I01 | 001 | `(10, 0.45, 26)` | 北東端（端ギミック） |
| stage13-I02 | 003 | `(-8.5, 1.45, 24)` | QTE木そばの木箱の上 |
| stage13-I03 | 004 | `(13, 0.45, -25)` | 南東端（端ギミック） |
| stage13-I04 | 005 | `(10, 3.85, -10)` | 高所台座3620（ダッシュ床で） |
| stage13-I05 | 006 | `(-13, 0.45, 17.5)` | レバーで開く西側隠し部屋 |
| stage13-I06 | 009 | `(-4, 0.45, 4)` | 北区画 西 |

DataIDに `007`/`008`（異常終了）は使用しない。

## その他の配置

- 木箱（cubeWoodSmall）・岩（base_rock1/2）— 足場・遮蔽として各区画に配置。
- 柵（fence）— 外周（南北 Z=±30、側面 X=±15）に24本。当たり判定なしの目印。
- 壁（collision_wall）— ゾーン境界の目印と遮蔽に4枚（3101/3102/3103/3105）。安全ルートは塞がない。
- 柵外景観（World 1: 木・岩・草）— インスタンシングで大量配置。
  - 草: `res/model/grass/grass1-3.csv`（スウェイ）／ 木: `res/model/tree2/lemonTree.Instancing.1-3.csv`
  - XFileList_simple.csv に `loadType=instancing` と PlacementCsv（ID 9200系）を登録。

## STAGES 定義への変更（_build_stage_grounds.py）

1-3 エントリ（line 56）を次に更新済み：
- `pits`: `((-11.0, 11.0, -13.0, -7.0),)`  ← 中央の堀
- `static_platforms`: `((10.0, -10.0, 3.0),)`  ← 堀上の高所台座3620を認識させる（必須）

## 地面モデル

- `stage_ground.x` は中央の堀（X −11〜+11, Z −13〜−7, 深さ20m）を持つ。
- `RED_FORTRESS_STAGE_GROUND='1-3'` で `_build_stage_grounds.py` をBlender実行して検証・再生成。

## 完了条件チェック

- [x] 通常走行・ジャンプ・近接攻撃だけで開始→全必須敵撃破→ゴール到達（検証スクリプトで安全ルート確認済み）
- [x] 必須敵は主要導線上・隠し場所なし・ピット上なし
- [x] 開始(G-北)・ゴール(G-南)周辺に回避不能ダメージなし
- [x] QTE木・ダッシュ床・移動床・レバー・感圧板・Y≥3静的床（アイテム）をすべて1以上
- [x] 敵11体≥10
- [x] 区画3（World 1目安「約3」）
- [x] 端ギミック2以上（北東I01・南東I03・QTE木・レバー）
- [x] 描画/物理CSVの座標・回転・倍率一致
- [x] 柵外に木・岩・草のインスタンシング景観
- [ ] ビルド（MSBuild Debug/x64）
- [ ] 手動プレイテスト（フェリー乗り継ぎ・台座への射出・感圧板の扉・安全ルート）

## ビルド・検証手順

1. `python tools/build_stage_1_3.py` — CSV一式を再生成（BOM付きUTF-8/CRLF）
2. `python tools/validate_stage_ground_only.py 1-3` — 地面検証（pit/配置/BFS安全ルート）
3. `RED_FORTRESS_STAGE_GROUND=1-3` で Blender 実行 → stage_ground.x 再生成＋検証
4. `MSBuild ... /p:Configuration=Debug /p:Platform=x64` → 0エラー
5. 手動プレイテスト

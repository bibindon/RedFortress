# STAGE_3_5 「ひゅんひゅんワープめいろ」設計書

## 基本情報

| 項目 | 値 |
|---|---|
| ワールド | World 3（草原・夕方） |
| サイズ | 120×240m（X: -60〜+60, Z: -120〜+120） |
| スタート位置 | `(0, 0.2, -112)` |
| ゴール位置 | `(0, 1.0, 112)` |
| 地面モデル | `stage_ground.x`（既存：Z=-55〜-45 に幅30mの落とし穴あり） |
| 空モデル | `../SkySphere_evening/SkySphere.blend.x` |

> **スタート・ゴール再考**: 旧デザインは start=(0,-28), goal=(0,114) だったが、
> メモ「まず、スタート位置とゴール位置を考え直す」に従い、ステージ全長を使う
> start=(0,-112), goal=(0,112) に変更する。プレイヤーは南端(Z=-120側)からスタート
> して北端(Z=+120側)のゴールへ向かう。

## 区画割り（5区画）

ステージをZ方向に5つの区画に分ける。各区画は壁で仕切り、ワープで移動する。
各区画の「色・目印」を変えて現在地を判別しやすくする。

```
Z=+120 ═══════════════════════════════════════
         区画E「ゴール前広場」 (Z: +60〜+120)
         床色: 標準 / 目印: ゴールポータル
Z= +60 ─────── 高さ2倍の壁（横一列）─────────
         区画D「ダメージ床の間」 (Z: 0〜+60)
         床色: 暗め / 目印: ダメージ床
Z=   0 ─────── 高さ2倍の壁（横一列）─────────
         区画C「中央交差点」 (Z: -45〜0)
         床色: 標準 / 目印: QTE木（シンボル）
Z= -45 ═══════════════════════════════════════
     （既存の落とし穴 Z=-55〜-45 を活用）
Z= -55 ═══════════════════════════════════════
         区画B「ワープ分岐地帯」 (Z: -120〜-55)
         床色: 標準 / 目印: 木箱の壁
Z=-120 ═══════════════════════════════════════
         区画A「スタート前室」 (Z=-120周辺)
```

各区画間の壁は横一列の高さ2倍の壁（collision_wall_tall）で遮断。
壁を越えるにはワープを使う。正規ルートは通常ジャンプ・走行・攻撃のみでクリア
可能だが、**ワープが唯一の区画間移動手段**となる（ワープは「触れると移動」なので
強化能力不要）。

## ワープネットワーク（迷宮の核）

### 設計思想
- 複数のワープペアが交差し、**どのワープを選んでも別のワープで戻れる**（一方通行なし）
- 正解ルート（区画A→B→C→D→E）以外に、**袋小路（敵部屋・アイテム部屋）**と
  **近道ショートカット**を用意
- 各ワープの出口付近に色やオブジェクトの目印を置き、現在地を判断できるようにする

### ワープペア一覧（10ペア = 20個）

| ペア | 入口(ID) | 座標 | 出口(ID) | 座標 | 役割 |
|---|---|---|---|---|---|
| P01 | warp01a | `(0, 0.2, -100)` | warp01b | `(-38, 0.2, -80)` | A→B（正解ルート第1歩） |
| P02 | warp02a | `(38, 0.2, -90)` | warp02b | `(0, 0.2, -60)` | B内移動（落とし穴前へ） |
| P03 | warp03a | `(-20, 0.2, -62)` | warp03b | `(-38, 0.2, -20)` | 落とし穴を飛び越えてB→C（**正解**） |
| P04 | warp04a | `(20, 0.2, -62)` | warp04b | `(38, 3.0, -10)` | 落とし穴を飛び越えてCの高所へ（**正解・高所ルート**） |
| P05 | warp05a | `(0, 0.2, -30)` | warp05b | `(-38, 0.2, 20)` | C→D（左ルート） |
| P06 | warp06a | `(0, 0.2, -30)` ※P05と同位置には置かない → `(5, 0.2, -28)` | warp06b | `(38, 0.2, 20)` | C→D（右ルート） |
| P07 | warp07a | `(-38, 0.2, 40)` | warp07b | `(-38, 0.2, 80)` | D→E（左・正解） |
| P08 | warp08a | `(38, 0.2, 40)` | warp08b | `(38, 0.2, 80)` | D→E（右・正解） |
| P09 | warp09a | `(-38, 0.2, -70)` | warp09b | `(-46, 0.2, 50)` | **近道**: B→D（ダメージ床地帯をショートカット） |
| P10 | warp10a | `(46, 0.2, -30)` | warp10b | `(46, 0.2, 100)` | **袋小路**: アイテム部屋へ（戻りワープ付き） |

### 戻り保証（一方通行なし）
- 各ペアは双方向ワープ（触れると対に移動）
- 袋小路（P10）も warp10b に触れれば warp10a に戻る
- 間違えたワープを選んでも、別のワープで必ず戻れる経路を維持

### 正解ルート
```
Start(0,-112)
 → 歩いて warp01a(0,-100) に触れる → warp01b(-38,-80) へ
 → 歩いて warp02a(38,-90)... ※B区内でワープを探す
 → warp03a(-20,-62) で落とし穴を越えて warp03b(-38,-20) へ (区画Cへ)
   または warp04a(20,-62) で高所へ
 → warp05a または warp06a で区画Dへ
 → warp07a または warp08a で区画Eへ
 → ゴール(0,112)
```

## 必須ギミック配置

### 1. ワープオブジェクト（WarpBears.csv）— メインギミック
上記10ペア（20個）。Y座標は基本的に0.2（地面すれすれ）。
P04の出口とP10のみ高所（Y=3.0）を使用。

### 2. 移動床（XFileListMove.csv）— 既存2つを再配置
| ID | 移動 | 用途 |
|---|---|---|
| 3509 | `(-18,0.4,-8) ↔ (18,0.4,-8)` | 区画C内の横移動補助 |
| 3510 | `(0,0.4,30) ↔ (0,3.5,30)` | 区画Dの昇降床（高所アイテムへの道） |

### 3. ダッシュブースター（DashBoosters.csv）
| ID | 位置 | 方向 | 用途 |
|---|---|---|---|
| booster01 | `(46, 0.5, -30)` | 上方向(0,1,0) | P10入口へ飛び上がる（高所ワープへ） |
| booster02 | `(0, 0.5, 50)` | 前方(0,0,1) Speed=18 | 区画Dのダメージ床を一気に越える近道 |

### 4. レバー（AttackTriggers.csv）— 任意だが迷宮感アップ
| ID | Type | 位置 | TargetID | Axis | 用途 |
|---|---|---|---|---|---|
| 1 | Lever | `(-38, 0.6, -20)` | 11001 | Y | 高所の静的床（ワープP04出口足場）を回転させて道を作る |
| 2 | Lever | `(38, 0.6, 20)` | 11002 | Y | 区画Dの壁を回転させて近道を開ける |

> レバー連動対象の attack_floor/attack_wall を XFileList_simple/Physics に登録。

### 5. QTE木（Interactables.csv + XFileList_simple/Physics）
| InteractionID | 位置 | 用途 |
|---|---|---|
| `stage35-tree-01` | `(0, 0, -30)` | 区画C中央のシンボル。素材報酬 |

描画: `../tree2/lemonTree.x`、衝突: `../tree2Physics/tree_cylinder_collision.x`

### 6. Y座標3以上の動かない床（static_platform）
| ID | モデル | 位置 | 用途 |
|---|---|---|---|
| 3601 | `static_platform_2x2` | `(38, 3.0, -10)` | ワープP04出口の足場（レバーで回転する床の代替landing） |
| 3602 | `static_platform_2x2` | `(0, 4.0, 30)` | 移動床3510の上のアイテム台座 |
| 3603 | `static_platform_1x1` | `(46, 3.0, -30)` | P10入口の高所足場 |

### 7. ダメージ床（LavaZones.csv + plateLava）
区画D（Z: 0〜+60）の一部をダメージ床にする。
| PhysicsID | 位置 | 用途 |
|---|---|---|
| 3701 | `(-20, 0, 40)` scale=1.0 | 区画D左側の危険地帯 |
| 3702 | `(20, 0, 40)` scale=1.0 | 区画D右側の危険地帯 |

ダメージ床をワープ(P07/P08)で避けるか、ダッシュブースター(booster02)で飛び越える。

### 8. 感圧板と連動扉（PressurePlates.csv）— 代替案
ダメージ床の代わりに感圧板＋連動扉を使う場合：
- 感圧板を踏んで扉を開け、別ルートを作る
- 上記ダメージ床案との二者択一（実装時に決定）

## 敵配置（World 3: 20体以上）

World 3の方針: `ghost`、`small_skeleton`、`skeleton` が主体。
bird・spider・ゴーレム系は**使用しない**。

| # | Type | 位置 | 区画 | 役割 |
|---|---|---|---|---|
| 1 | ghost | `(-30, 0.2, -100)` | A | スタート前室の遠距離敵 |
| 2 | small_skeleton | `(30, 0.2, -100)` | A | スタート前室の近接敵 |
| 3 | skeleton | `(-30, 0.2, -80)` | B | ワープP01出口付近 |
| 4 | ghost | `(30, 0.2, -70)` | B | B区画中央 |
| 5 | small_skeleton | `(-20, 0.2, -70)` | B | P09入口の守り |
| 6 | skeleton | `(30, 0.2, -60)` | B | 落とし穴前 |
| 7 | ghost | `(-38, 0.2, -30)` | C | ワープP03出口 |
| 8 | skeleton | `(38, 3.0, -8)` | C(高所) | P04出口の高所 |
| 9 | small_skeleton | `(0, 0.2, -20)` | C | QTE木の近く |
| 10 | ghost | `(-20, 0.2, -10)` | C | C区画中央 |
| 11 | skeleton | `(20, 0.2, -10)` | C | C区画中央 |
| 12 | small_skeleton | `(0, 0.2, -30)` | C | QTE木周辺 |
| 13 | ghost | `(-38, 0.2, 10)` | D | D区画左 |
| 14 | skeleton | `(38, 0.2, 10)` | D | D区画右 |
| 15 | small_skeleton | `(-20, 0.2, 30)` | D | ダメージ床付近 |
| 16 | ghost | `(20, 0.2, 30)` | D | ダメージ床付近 |
| 17 | skeleton | `(-38, 0.2, 50)` | D | P07入口 |
| 18 | ghost | `(38, 0.2, 50)` | D | P08入口 |
| 19 | skeleton | `(-30, 0.2, 90)` | E | ゴール前 |
| 20 | small_skeleton | `(30, 0.2, 90)` | E | ゴール前 |
| 21 | ghost | `(0, 0.2, 100)` | E | ゴール直前（最後の妨害） |
| 22 | skeleton | `(-20, 0.2, 105)` | E | ゴール前 |
| 23 | small_skeleton | `(20, 0.2, 105)` | E | ゴール前 |

**計23体**（20体以上クリア）

## 収集物（Collectibles.csv）

| ID | Type | DataID | 位置 | 備考 |
|---|---|---|---|---|
| stage35-I01 | Item | 001 | `(-46, 0.45, -70)` | B区画・ワープP09近く |
| stage35-I02 | Item | 006 | `(46, 3.45, -30)` | **高所**・P10袋小路（ブースターで到達） |
| stage35-I03 | Item | 011 | `(0, 4.45, 30)` | **高所**・移動床の上の台座 |

> I02/I03は強化能力なしでも到達可能：
> - I02: ダッシュブースターbooster01で(46,-30)の高所へ
> - I03: 移動床3510でY=3.5まで昇降し、台座(3602, Y=4.0)へジャンプ

## 破壊可能オブジェクト（Destructibles.csv）

| 位置 | HP | 用途 |
|---|---|---|
| `(-40, 0, -90)` | 3 | B区画の行き止まりを塞ぐ |
| `(40, 0, -65)` | 3 | 落とし穴前の通路 |
| `(-40, 0, -20)` | 3 | C区画のワープP03出口付近 |
| `(40, 0, 45)` | 3 | D区画のダメージ床横 |

## ポイントライト（PointLights.csv）

夕方の草原だが、ワープの目印として各区画にライトを配置。

| 位置 | 色 | 用途 |
|---|---|---|
| `(0, 3, -100)` | 暖色(1.0,0.8,0.4) | スタート前室 |
| `(-38, 3, -20)` | 青(0.3,0.6,1.0) | C区画・ワープP03出口 |
| `(38, 3, -10)` | 緑(0.3,1.0,0.5) | C区画高所・P04出口 |
| `(0, 3, 40)` | 赤(1.0,0.3,0.2) | D区画・ダメージ床警告 |
| `(0, 3, 100)` | 金(1.0,0.85,0.3) | ゴール前 |

## 柵外の景観（World 3: 木・岩・草）

インスタンシング方式で配置：
- 草: `../grass/grass.x` + 配置CSV（約300個）
- 木: `../tree2/lemonTree.Instancing.x` + 配置CSV（約60本）
- 岩: `../base/base_rock1.x`, `base_rock2.x` を数個

回転・倍率をばらつかせ、密集と疎のむらを作る。

## 区画間の壁（collision_wall_tall）

Z方向の区画切りを横一列の高さ2倍の壁で行う：

| Z位置 | 壁の並び（X間隔8m） |
|---|---|
| Z=+60 | X=-56,-48,-40,...,+56（15枚） |
| Z=0 | X=-56,-48,-40,...,+56（15枚） |

> 落とし穴（Z=-55〜-45）は既存の地面モデルにあるため、壁ではなく穴でB-C間を分断。

## 既存データからの変更点まとめ

| 項目 | 旧 | 新 |
|---|---|---|
| スタート | (0,0.2,-28) | (0,0.2,-112) |
| ゴール | (0,1.0,114) | (0,1.0,112) |
| 敵数 | 8体 | 23体 |
| 敵種類 | bird×2, small_golem×2混入 | ghost/skeleton/small_skeleton のみ |
| ワープ | 7ペア（一本道） | 10ペア（迷宮・分岐・袋小路） |
| QTE木 | なし | あり（区画C中央） |
| ダッシュブースター | なし | 2つ |
| レバー | なし | 2つ（任意） |
| Y3以上の静的床 | なし | 3つ |
| ダメージ床 | なし | 2つ（区画D） |
| ポイントライト | なし | 5つ |

## 完了条件チェック（STAGE_GENERATION_MEMO.md準拠）

- [x] QTE木 1つ以上
- [x] ダッシュブースター 1つ以上（booster01で取れないアイテムI02へ到達）
- [x] 移動床 1つ以上（3510で高所アイテムI03へ到達）
- [x] レバー 1つ以上
- [x] ワープ 1つ以上（メインギミック）
- [x] Y座標3以上の動かない床 1つ以上（3601〜3603）
- [x] 敵 World 3: 20体以上（23体）
- [x] ステージの端にギミック2つ以上（ワープP09/P10が端、破壊可能箱が端）
- [ ] 正規ルートが通常操作のみでクリア可能（実装後にプレイテスト）
- [ ] Debug|x64 ビルド成功

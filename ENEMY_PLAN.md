# おばけ・鳥・カニ・カエル敵追加計画

## 目的

現在は同じ犬モデルを流用している敵システムへ、CC0でクレジット表記不要の次の4種類を追加する。

- おばけ: Quaternius「Ghost」
- 鳥: mess110「Bird」
- カニ: methodical pixel「Crab low poly animated 3d model」
- カエル: methodical pixel「Frog low poly animated 3d model」

既存ステージとの互換性を保つため、既存の`wolf`および`enemy2`～`enemy6`は残し、新しい種類名として`ghost`、`bird`、`crab`、`frog`を追加する。

## 現状

- `EnemyManager`は全敵について`wolfAnim.x`と`wolfAnim.csv`を読み込む。
- `EnemyBase`が`idle`、`walk`、`creep`、`run`を直接再生する。
- 全敵が円柱当たり判定を使用し、毎フレーム重力を受ける。
- 接触攻撃の判定は`GameApp`、索敵・追跡・退却・被弾・死亡は`EnemyBase`が担当する。
- ステージ配置は各ステージの`EnemyPositions.csv`で管理される。
- ステージエディターの敵種類一覧は固定値である。
- ステージエディターで種類を変更しても、現在は種類名だけが変わり、モデルや実体は再生成されない。

## 実装方針

### 1. 配布物の取得と検査

各配布物を取得し、元ファイル、テクスチャ、リグ、アニメーション、CC0表記を確認する。各リソースフォルダーには、ゲーム内表示用ではなく由来を追跡するための`SOURCE.md`を保存する。

検査項目:

- メッシュとテクスチャが欠落していないこと
- アーマチュアとウェイトが正常であること
- アニメーション名とフレーム範囲
- モデルの正面方向、原点、寸法
- CC0であること
- 公式Blender DirectX Xエクスポーターで書き出せること

### 2. ゲーム用リソースの作成

次のフォルダーへ配置する。

```text
RedFortress2/MultiPassRendering/res/model2/Ghost/
RedFortress2/MultiPassRendering/res/model2/Bird/
RedFortress2/MultiPassRendering/res/model2/Crab/
RedFortress2/MultiPassRendering/res/model2/Frog/
```

Blender公式アドオンの`bpy.ops.export_scene.directx_x`だけを使い、基準メッシュとアニメーション別の`.x`を出力する。独自の`.x`変換・再シリアライズ処理は作らない。

論理アニメーション名は次へ統一する。

- `idle`
- `move`
- `fastMove`
- `attack`
- `hit`
- `death`

元モデルに存在しないアニメーションには、安全な代替アニメーションを設定する。すべての`.x`はBOMなしUTF-8、CRLFとする。

座標と寸法:

- Y軸を上方向とする。
- 1ユニットを1メートルとする。
- カニとカエルの原点は接地面中央とする。
- 鳥とおばけの原点は胴体中央とする。
- 正面方向の差は可能ならBlender側で修正し、必要な場合だけ定義データに補正角度を持たせる。

### 3. 敵定義の拡張

`EnemyManager::FactoryEntry`を拡張し、敵ごとに次を設定できるようにする。

- モデルパス
- アニメーションCSVパス
- 表示倍率
- HP
- 移動速度
- 視認距離
- 接触半径
- 当たり判定高さ
- 地上型または飛行型
- アニメーション対応
- 死亡後の削除待機時間
- モデル正面の補正角度

`wolfAnim.x`と`wolfAnim.csv`の固定指定を廃止する。既存の犬型敵は同じ定義構造へ移すが、挙動と数値は変えない。

追加予定クラス:

- `EnemyCrab`
- `EnemyFrog`
- `EnemyBird`
- `EnemyGhost`

### 4. アニメーション状態の拡張

共通状態を次へ拡張する。

- 待機
- 低速移動
- 高速移動
- 攻撃
- 被弾
- 死亡

接触ダメージ発生時に攻撃状態へ入り、撃破時は死亡アニメーションの再生後にメッシュを削除する。該当アニメーションがないモデルは代替アニメーションと既存の点滅処理を使う。

### 5. 地上敵

#### カニ

- 既存の地上衝突と重力を利用する。
- 低く幅広い円柱当たり判定にする。
- 犬より遅く、接触範囲を少し広くする。
- 踏みつけ可能にする。
- 横歩きらしさはモデルの移動アニメーションと向きで表現する。

#### カエル

- 地上衝突と重力を利用する。
- 一定間隔で跳躍する。
- 着地を物理判定で確認してから次の跳躍へ進む。
- 跳躍中にだけ大きく水平移動する。
- 踏みつけ可能にする。

### 6. 飛行敵

飛行敵は重力を無効にし、スポーン高度を基準とする上下範囲を持つ。初期実装では地形をすり抜けない。

#### おばけ

- ゆっくり上下しながら浮遊する。
- プレイヤー発見後は緩やかに直接追跡する。
- 接触後は少し退いてから再追跡する。
- 半透明は描画順問題を避けるため初期実装では使わず、不透明なモデルとして表示する。

#### 鳥

- 待機中はスポーン地点付近を旋回する。
- プレイヤー発見後は急降下する。
- 接触または地面付近まで降下した後は上昇して再攻撃に備える。
- 到達不能な高度へ移動しないよう上下限を設ける。

鳥とおばけも、プレイヤーが上から下降して接触した場合は踏みつけ可能とする。

### 7. ステージエディターと配置

敵種類一覧へ次を追加する。

```text
ghost
bird
crab
frog
```

種類変更時は文字列だけを書き換えず、同じ位置と向きを保持して正しいモデル・敵クラスを再生成する。

最初は専用テスト配置で4種類を同時に確認する。正常動作を確認した後、既存ステージの犬を必要な箇所だけ新しい敵へ置き換える。

### 8. 検証

各敵について次を確認する。

- モデル、マテリアル、テクスチャの表示
- 正面方向、倍率、原点、接地または浮遊位置
- 待機、移動、攻撃、被弾、死亡
- 壁、床、段差、崖との衝突
- プレイヤーへの接触ダメージ
- 通常攻撃、踏みつけ、スター、爆弾、バスターからの被ダメージ
- HPバーの位置
- アイテムドロップ
- 死亡後のメッシュ削除
- ステージ再読込とステージ切替
- 複数体表示時の負荷
- 既存犬型敵の回帰

ビルド時は環境変数を`Path`へ統一し、64ビットのMSBuildで`Debug|x64`を使用する。実行時はリソースが出力先へコピーされ、作業ディレクトリから相対パスを解決できることを確認する。

## 実装順序

1. 配布物を取得してモデルを検査する。
2. 4モデルを公式エクスポーターで`.x`へ変換し、単体表示を確認する。
3. 敵定義からモデル・アニメーション・当たり判定を選べるようにする。
4. カニを既存地上AIで動かす。
5. カエルの跳躍を実装する。
6. 共通飛行移動を実装する。
7. おばけの浮遊追跡を実装する。
8. 鳥の旋回・急降下を実装する。
9. 攻撃・被弾・死亡アニメーションを統合する。
10. ステージエディターとCSV配置を対応させる。
11. `Debug|x64`ビルドとゲーム内回帰試験を行う。

## 完了条件

- `ghost`、`bird`、`crab`、`frog`をCSVとステージエディターの両方から配置できる。
- 4種類が固有モデルと適切な地上・飛行挙動で動作する。
- プレイヤーとの戦闘、死亡、ドロップ、ステージ切替が正常に動作する。
- 既存の犬型敵と既存ステージに退行がない。
- `Debug|x64`ビルドが成功する。

## 初回実装の進捗

2026年7月27日時点で、次の初回実装まで完了した。

- 4モデルを取得し、CC0であること、メッシュ、リグ、アニメーションを確認した。
- Blender 5.1の公式DirectX Xエクスポーターで、基準メッシュと各アニメーションを出力した。
- `ghost`、`bird`、`crab`、`frog`の敵クラスと登録処理を追加した。
- カニの地上移動、カエルの跳躍移動、おばけの浮遊追跡、鳥の飛行追跡を追加した。
- 攻撃、被弾、死亡アニメーションを既存の戦闘処理へ接続した。
- ステージエディターの種類一覧と、種類変更時のモデル再生成に対応した。
- stage1へ4種類の確認用配置を追加した。
- `Debug|x64`ビルドが成功した。

次の確認作業は、実際のゲーム画面で倍率、向き、接地高度、当たり判定、アニメーション速度を調整することである。調整後、各ステージの犬型敵を意図に合わせて置き換える。
